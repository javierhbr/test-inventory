# Configuration Components

This directory contains the main components responsible for application settings and configuration:

- `SystemConfiguration.tsx`: Manages system-wide configurations, such as domains and DSLs.
- `UserManagement.tsx`: Allows administrators to manage users and their roles.
- `PermissionsManager.tsx`: Utility for handling and assigning permissions within the system.
